<header class="header">
  <nav class="navbar dark-gray-bg navbar-fixed-top">
    <div class="vertical-middle">
      <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
        <div class="row">
          <div class="col-md-3">
              <div class="navbar-header">
                  <a class="navbar-brand" href="#">
                      <h2 class="white-text">ZANALYS</h2>
                  </a>
              </div>
              <button type="button" class="navbar-toggle" data-toggle="collapse" aria-expanded="false">
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
          </div>
          <div class="col-md-9">
            <div class="collapse navbar-collapse">
              <ul class="nav navbar-nav navbar-right localize-lang-droplist">
                <!-- Localize droplist -->
                <li class="dropdown locale-dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    <% if I18n.locale == :en %>
                      <span data-value="en"><%= "English" %></span>
                    <% elsif I18n.locale == :fr %>
                      <span data-value="fr"><%= "French" %></span>
                    <% end %>
                    <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                      <li>
                        <%= button_to "English", change_locale_path(:en), class: "btn btn-link a-to-btn" %>
                      </li>
                      <li>
                        <%= button_to "French", change_locale_path(:fr), class: "btn btn-link a-to-btn" %>
                      </li>
                  </ul>
                </li>
                <!-- <li class="dropdown role-dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    <span data-value="en"><%#= Role %></span>
                    <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li>
                      <%#= select_tag :user_role, options_for_select(current_user.roles.collect { |r| [r.name_en, r.id]}),{class: "form-control selectpicker",data: {live_search: true} }%>
                    </li>
                  </ul>
                </li> -->
                <!-- <li>
                    <form action="#" method="#">
                        <div class="search">
                            <input type="text" class="form-control header_search" id="search" placeholder="<%#= I18n.t('search') %>">
                        </div>
                    </form>
                </li> -->
                <li class="dropdown">
                    <div class="profile vertical  dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <div class="vertical-middle">
                            <a href="#">
                                <%= image_tag current_user.photo.url,class: "img-circle img-responsive",alt: "" %>
                                <span><%= current_user.full_name || current_user.email %></span>
                            </a>
                        </div>
                    </div>
                    <ul class="dropdown-menu">
                        <li><%= link_to "Profile",edit_user_registration_path %></li>
                        <li>
                          <%= select_tag :user_role, options_for_select(current_user.roles.collect { |r| [r.name_en, r.id]},[current_role.id]),{class: "form-control",prompt: "select role" }%>
                        </li>
                        <li><%= link_to 'Log out', destroy_user_session_path, method: :get %></li>
                    </ul>
                </li>
                <%= render 'layouts/shared/user_notifications' %>
                <!-- <%# if @notifications.present? %>
                  <li class="dropdown">
                      <div class="profile vertical  dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                          <div class="vertical-middle">
                              <a href="#">
                                  <i class="fa fa-bell-o"></i>
                                    <%# if @notifications.present? %>
                                      <span class="label label-warning notification">New</span>
                                    <%# else %>
                                      <span class="label notification"></span>
                                    <%# end %>
                              </a>
                          </div>
                      </div>
                      <ul class="dropdown-menu">
                        <%# if @notifications.present? %>
                            <%# @notifications.each do |notification| %>
                              <li><a href='<%#= notification.url %>' onclick="setNotificationToFalse(this)"><%#= notification.message %></a></li>
                            <%# end %>
                        <%# else %>
                        <li>No notifications</li>
                        <%# end %>
                      </ul>
                  </li>
                <%# end %> -->
              </ul>
            </div>
          </div>
        </div>
          <!-- /.navbar-collapse -->
      </div>
    </div>
      <!-- /.container-fluid -->
  </nav>
</header>
